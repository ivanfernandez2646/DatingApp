export interface UserParams{
    pageNumber: number;
    pageSize: number;
}

export interface PaginationHeader{
    pageNumber: number;
    totalPages: number;
    pageSize: number;
    totalCount: number;
}

